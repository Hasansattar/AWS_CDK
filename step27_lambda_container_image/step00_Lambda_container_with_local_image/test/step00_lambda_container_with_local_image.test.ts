// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as Step00LambdaContainerWithLocalImage from '../lib/step00_lambda_container_with_local_image-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/step00_lambda_container_with_local_image-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new Step00LambdaContainerWithLocalImage.Step00LambdaContainerWithLocalImageStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
